language: php

php:
  - 7.0

matrix:
  fast_finish: true

mysql:
  database: mmenavas_asu_brand_test
  username: root
  encoding: utf8

before_install:
  - sudo apt-get update > /dev/null

install:
  - phpenv config-rm xdebug.ini
  - echo 'sendmail_path = /bin/true' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - composer self-update
  - composer install --no-dev
  - composer global require drush/drush:8.*
  - export PATH="$HOME/.composer/vendor/bin:$PATH"

before_script:
  # check current directory
  - pwd
  # navigate out of module directory to prevent blown stack by recursive module lookup
  - cd ../..
  # Create DB
  - mysql -e 'create database mmenavas_asu_brand_test'
  # Run drush
  - drush status
#  - php -d sendmail_path=`which true` ~/.composer/vendor/bin/drush.php --yes core-quick-drupal testsite --no-server --db-url=mysql://root:@127.0.0.1/mmenavas_asu_brand_test mmenavas_asu_brand_test  --profile=minimal
#  - ln -s $(readlink -e $(cd -)) mmenavas_asu_brand_test/drupal/modules/mmenavas_asu_brand
#  - cd travis_ci_drupal_module_example_test/drupal
#  - drush --yes pm-enable mmenavas_asu_brand

#script: drush test-run 'Travis-CI Drupal Module Example' --uri=http://127.0.0.1:8080
script: echo "Hello world"
